/**
 * Array
 */

const member = ['a', 'b', 'c', 'd', 'e'];
console.log(member);
console.log(member[2]);

console.log('--------------------');


for(let element of member) {
  console.log(`${element} ${member}`);
}